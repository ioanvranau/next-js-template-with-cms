<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This project uses **Next.js 16** + **React 19** + **Tailwind CSS v4**. APIs,
conventions, and file structure may differ from your training data. When unsure,
read the relevant guide in `node_modules/next/dist/docs/` before writing code,
and heed deprecation notices. Tailwind v4 is **CSS-first**: there is no
`tailwind.config.js` — theme tokens live in `app/globals.css` under `@theme`.
<!-- END:nextjs-agent-rules -->
